# Myynd intelligence work log

## Goal · 14 September 2026

Make the attention feed useful: recent personal requests and concrete project actions, understandable cards that open their real source, and durable learning from Done / Not relevant. Keep routine deliveries and subscription updates in the Brief. Ground chat and delegated work in current project memory. Simplify the Automations heading.

## Investigation

- The screenshot is evidence of an unwanted card, not an instruction to update `CLAUDE.md` or replay bot sessions.
- Multiple producers create attention items: the feed, the Brief, and automations. Their admission and feedback rules need to agree.
- Existing feed filtering allows undated and old unread documents. Newly indexed files are described as newly arrived even when their contents are historical.
- The Brief currently removes all bulk/transactional mail, so useful delivery and subscription updates have no appropriate destination.
- Work is divided between feed/feedback, chat/memory/delegation, navigation/UI, and Brief/integration. Each area includes focused regression checks.

## Changes and verification

### Implemented

- Shared source classification uses the source's date (seven-day attention window), distinguishes personal mail from promotions and service updates, and excludes CVs and internal agent artifacts. A newly indexed document no longer acquires a new event date.
- Feed generation requires an exact quote supporting the requested action, a clear task and explanation, and a real document identity. Empty output is valid.
- Durable feedback retains a source snapshot when feed items or tasks are completed, dismissed or deleted, including after source removal/reindexing. Related-request matching is scoped; dismissing one human message does not silence that person's unrelated requests. Undo withdraws the corresponding suppression.
- The Brief has a separate service-updates section and cannot create tasks from deliveries, unsupported project guesses or agent instructions. Real tasks retain their explanatory note.
- Existing cached feed items and untouched Brief suggestions receive the same relevance checks on read. Filtering preserves underlying history and user-adopted tasks.
- Chat includes current projects, goals, task state and direction across provider paths, including conversations with no matching documents. Project extraction requires evidence from the person's own words. Consolidation preserves manually written memory and does not promote guesses into fact.
- Delegation returns its final deliverable without intermediate search chatter, reports truncated work, and treats email as email and other documents as task material.
- Original-source navigation opens web links on the user's device. Missing message identity no longer promises to open an email by opening a generic inbox. Gmail preserves source/account context; Outlook imports the provider's message link.
- Gmail and Outlook now preserve read/sent/bulk metadata used by selection. Outlook's documented `webLink` field is the original-message destination ([Microsoft Graph](https://learn.microsoft.com/en-us/graph/api/message-get?view=graph-rest-1.0)).
- Automation page heading is “Your automations” once. Built-in reply-detection automations use the same feedback and recency rules, exclude answered threads, and filter before candidate limits. Explicit custom automation scopes remain intact.
- Read-only mail connections show “Copy draft and open email” instead of a Send button they cannot fulfil. SMTP sending keeps its existing confirmation flow. Copying/opening does not send or complete a task.
- Save-time checks prevent a late model result from reviving an item dismissed during generation. Manual memory changes during consolidation also win over the in-flight result.

### Final verification

- Baseline: 1,047 tests, 1,029 passed, 18 skipped, zero failures.
- Full combined suite: **1,112 tests; 1,094 passed, 18 skipped, zero failures**. New cases cover stale imports, CVs, screenshot-style agent instructions, genuine personal code-review requests, delivery routing, exact source actions, durable feedback, source removal/restart, Undo, unrelated future requests, malformed model output, manual task preservation, and in-flight changes.
- `npm run build` passed, including client/server/desktop TypeScript checks and the production web build. Vite reports the existing large-bundle advisory; compilation succeeds.
- Isolated real-server API tests verified exact repository/email destinations, missing-source behavior, authentication, and mail-sending capability reporting.
- Mocked Gmail/Outlook synchronization tests verified read/sent/list flags, source identity, Outlook provider URLs, and Gmail account-aware message search links.
- Browser smoke used isolated API fixtures at desktop and 390px width. Verified readable card layout, one automation heading, Brief delivery updates, and actual navigation to the fixture's exact email/repository/delivery destinations. Browser popup/native paths additionally have unit coverage.
- Independent agent review identified and resolved the save-time feedback race, memory edit race, unsupported schema constraint, description-length mismatch, and unsupported mail-send UI.

### Scope and practical limits

At the end of the initial implementation stage, these changes were in the source workspace and web production build; the desktop package had not yet been replaced. Those first tests used isolated temporary data and simulated model/mail providers. The subsequent installation and live checks are documented below.

The default attention window is seven days from the source timestamp. Older documents remain searchable when explicitly needed. Project extraction fills new/empty goals from explicit user statements; it preserves existing goals and closed projects, which remain editable in Memory. Model quality still depends on the configured provider and connected source coverage. Repository execution remains tied to the app's existing supported tool and permission flow; a generated draft is not reported as an external action completed. Gmail/Outlook read access alone does not grant in-app sending; the UI hands the prepared draft to the user's email app.

## Installation and real-workflow evaluation — 14 September 2026

The follow-up evaluates the configured local Ollama model (`qwen3.5:9b`) against a private SQLite snapshot of the actual account, then checks the packaged app with the original profile. Snapshot results and prompts remain outside the repository. No outgoing email or external work was sent as part of evaluation.

### Findings that changed the implementation

- The account contained 219 indexed documents: 211 older than seven days, three recent technical files, three bulk promotions, one sent message, and one service receipt. There was no eligible recent human request in that snapshot. An empty task feed is the correct result; the receipt belongs in the Brief.
- The real model misnamed the user's website, omitted a saved project and promoted an old audit into current work. Explicit project-overview questions now return the exact project registry, including missing goals and provenance, without model invention. The repeat completed in 13 ms and listed the four canonical projects correctly.
- A legacy inferred project duplicated the name and goal of an existing manual project. Context now suppresses that alias while preserving stored records. Named-project requests scope to that project's context, with an explicit goal owner.
- Delegation exceeded its first-response timeout when the local model received full-sized context. Initial evidence is now bounded, with deeper source reading still available through tools. The same task completed in 36 seconds after this change. Further evaluation checks whether it delivers a useful plan as well as responding.
- The real Brief repeated a saved goal without a current event and omitted the receipt. On a repeat, it reversed who paid whom. Project updates now need an openable source and cannot restate the objective. Service updates use the original subject and source link, avoiding generated payment claims.
- Notes appeared healthy although its refresh was blocked by macOS disk access. Sources now shows the missing access and opens its existing access instructions.
- Automation discovery used old index evidence, and renamed suggestions could return after dismissal. Proposals, cached results and adoption now recheck current evidence, feedback, source/search scope and project state. Dismissals remember the workflow, not just the displayed name. Explicitly requested historical automation scopes remain supported.
- News now uses active work context and durable read/dismiss feedback; stale Brief projects and unadopted generated tasks no longer manufacture interests.
- Map clicks previously selected a whole source group. The map now separates projected nodes and labels while preserving its overall shape; selected documents have actual content, date, project context, feedback, nearby documents and an original-source action.

### Recovery point

The previous app bundle, account data and desktop session profile were backed up privately under `~/.myynd-backups/20260914-before-0.2.12` after quitting the app. The account's source credentials and data are excluded from the packaged app and this log.

### Native app and model acceptance checks

- Installed version 0.2.12 in `/Applications/Myynd.app` and relaunched with the original session and account. Native UI checks confirmed the empty attention feed, correct four-project chat answer, the single “Your automations” heading and the source-access status. Refreshing the real mail connection completed successfully.
- Saved the user's explicitly stated preferences through the native Memory UI, preserving the existing instruction against fabricated data: prioritize recent direct human requests and active project steps, keep old reference files and bulk mail out of the feed, route routine service updates to the Brief, respect completed/dismissed work, and require clear actions with their original sources. This is editable user memory shared with chat and automation context.
- Selected the actual CV in the knowledge map. Its detail panel showed its contents, source date, project and existing dismissal. “Take me to it” opened the original local PDF in Preview. The CV remains accessible as knowledge without returning as a suggested task.
- Selected the actual service receipt and used “Take me to it.” Apple Mail opened the exact matching message in the correct account. No message was sent. This verifies native email navigation as well as local-file navigation.
- Follow-up map inspection exposed a dense highlight of every document in the selected source group. Selection now highlights the selected document and its actual neighbors; source dates include the year.
- The receipt's map panel incorrectly linked it to the website project through generic goal words. Project associations now require exact name/domain evidence or an established document-to-project task link. The actual receipt no longer carries that false label. Long tracking URLs are removed from the short preview; original-source actions appear above the excerpt so they remain reachable on a smaller window.
- Native Memory inspection showed a legacy inferred project alias and an open-task count that disagreed with To do. Memory now lists the canonical projects while keeping legacy records accessible for linked tasks; open counts and next-step context use the same visible task set as To do. Completed history and underlying records are preserved.
- The Brief had already reached its existing daily generation allowance. Fresh service updates now refresh independently of that allowance, without another model call. Cached Brief text cannot hide a newly arrived receipt.
- The native News panel remained in “Reading the papers…” with no active work after its daily selection allowance was exhausted. Fixed the asynchronous no-op state that caused each poll to claim work was running. Regression checks cover repeated requests, absent project context and forced refresh at the allowance.
- Tested a delegated next-steps note using the actual local model and saved H-Farm goal. Early results pulled unrelated files and old school announcements into current work. Current-project planning now requires recent, relevant, unsuppressed evidence across initial retrieval and subsequent tools; an explicit historical request can still access the archive. The final repeat completed in 33 seconds, marked its steps as proposals and stated that current status could not be verified. Its plan remained general because there was no fresh project evidence. This is improved grounding, not evidence of expert-level autonomous planning.
- Automation creation was tested in a private copy of the account and never activated as part of this evaluation. The first real-model response failed the recipe schema. Subsequent drafts demonstrated why schema checks alone are insufficient: they changed the daily 08:00 schedule to an arrival trigger, requested a prompt rather than an internal note, or split one summary into multiple notes. Generation now explains field meanings directly, validates the explicit requirements and allows at most one repair before returning an error without saving an invalid recipe.
- The final real-model creation trial passed without needing repair: daily 08:00, only new mail, one prepared internal note, only the permitted mail-reading tool, the requested H-Farm scope and exclusions, exact source links and no sending. Its recipe was kept paused in the private snapshot. A normal no-matching-source run remains quiet and reports that nothing qualified; it does not manufacture an empty task.
- Explicit current-human-email workflows now carry a saved selection policy, separate from inferred automation proposals. This policy is enforced before candidate limits and carried into delegated searches and source reads, with another feedback check before saving the result. Explicit historical/order workflows remain possible.

### Current quality limits

- Notes has 88 cached documents but needs macOS disk access to refresh. GitHub and Calendar are not connected. These missing inputs limit current-work awareness and repository actions.
- Two saved projects have no goal; the app now says so rather than borrowing another project's goal. Existing inferred memory remains visible and editable; this evaluation does not silently rewrite the user's personal history.
- The configured local model remains `qwen3.5:9b`. Actual generation trials took roughly 33–76 seconds and revealed factual and schema errors that fixture tests alone did not catch. Deterministic facts, source checks, schema validation and explicit unknowns now constrain the paths tested here. Broader long-running execution quality has not been proven by these checks.

### Final installed acceptance

- **Myynd 0.2.12 is installed in `/Applications/Myynd.app` and running.** The original account, connected-source configuration, history and session were retained.
- Full combined suite: **1,165 tests, 1,147 passed, 18 skipped, zero failures**. The production build and all three TypeScript configurations pass. A final visual-only adjustment moved map source actions above the preview; the production build was rerun afterward.
- Compared all **92 packaged runtime/web files**, including 75 server modules, with the workspace/build outputs. The installed bundle passes macOS strict deep signature verification. Removed two obsolete generated web assets left by the intermediate overlay copy before the successful signature check.
- Final native restart confirmed: the feed contains no stale suggestions; the Brief contains the actual service receipt even at the model allowance; News finishes with its honest empty state; Memory lists the four canonical projects, consistent open-task counts and the saved preferences; the map shows non-overlapping labels, a full source date, precise selection, readable source text and visible original-source actions. The false receipt-to-website association is gone. Left the expanded map open for review.
- Earlier in this same installed-session evaluation, source actions opened the exact actual email in Apple Mail and original PDF in Preview. No outgoing messages were sent, no test automation was enabled in the real account, and the private test automation remained paused.
- Quality assessment: relevance and grounding improved on the observed failures; exact project recall and the final automation-creation trial passed. Complex delegated project planning remains general when only a broad goal and historical documents are available. Missing current project evidence, source access and goals are explicit limitations, not reported as completed work.

## Phase 3 — read-only knowledge and attention audit

Checked the active legacy-root account on 14 September 2026 at 23:18 UTC. The live SQLite database was opened read-only; imported application logic ran only against a private temporary backup, which was then removed. No account data, source scope, feedback or tasks were changed by this audit, and no document bodies or unrelated note titles were printed.

- The account currently has 221 indexed documents: 97 local files, 88 Notes and 36 emails. Its existing local-source setting is “whole computer,” covering the home folder and iCloud documents. Indexing accepts readable spreadsheets as source material; inclusion does not itself establish a task or a project relationship.
- The specifically requested `Beer.xlsx` is `~/Downloads/Beer.xlsx`, modified on 19 September 2025 and indexed on 11 September 2026. Only its metadata was inspected. It is classified as historical reference material, has no related project, and is ineligible for the attention feed.
- Of the 36 imported emails, seven have source dates within the last seven days: five bulk/list messages, one sent message and one service receipt. There are zero current human-email candidates under the attention rules. The receipt belongs in the Brief. This describes the imported dataset, not every message that might exist in the underlying mailbox.
- Current knowledge classifications are 196 old sources, 11 completed, five dismissed, five bulk/list messages, two technical files, one already-sent message and one service update. The stored feed has 41 completed and 33 dismissed items, with no open item. Recalculation therefore produces no attention card from these sources. This explains the empty feed; it does not by itself prove that proactive project planning meets the user's expectations.
- Map lines are visual associations from distinctive shared words. Related-project labels use separate identity/evidence rules. Chat searches for a relevant subset of indexed sources and applies named-project and workflow constraints; indexing does not mean every document is consulted for every answer.
- Added a brief explanation to both map views and the original local-file location to selected document details. The underlying graph, inclusion scope and attention rules are unchanged. Client typecheck and 22 focused map, association and localization checks pass.

### Reliable chat saves and provider waiting

- The reported goal-edit failure had two distinct causes: compatible chat stopped waiting after 15 seconds, and project learning ran only after a completed answer, deliberately refusing to overwrite existing goals. Explicit project/goal statements now take a synchronous, validated write path before the acknowledgement. Existing goals, new projects, renames and explicit state changes use the same durable registry as Memory and context. Conflicting batches roll back; vague requests ask for the missing values without claiming a save. Quoted source instructions remain ineligible.
- Compatible chat now allows 90 seconds for a local provider’s initial response and 60 seconds for a remote provider. Cancellation aborts the existing request; there is no automatic duplicate generation. A controlled test advances past the former 15-second limit, verifies exactly one live request, completes it, then verifies cancellation on a separate request.
- ChatGPT subscription selection now has its own provider identity and precedes retained API/compatible settings. Chat and delegated work skip the Claude subscription branch when ChatGPT is selected. The existing guarded Myynd tool paths remain available, and selection does not silently fall back to API billing.
- A real ChatGPT planning trial exposed a missed phrase (“what should I work on next”) and an unfiltered later chat search. Named current-project planning now applies the existing freshness, relevance and durable-feedback rules to initial retrieval and every subsequent chat search. Earlier generated assistant claims cannot re-enter as current evidence. With no matching current source, the prompt limits its statement to what was found in connected sources and asks for clearly labelled proposals from the saved goal. Explicit historical questions retain access to archived documents.
- Verification: server TypeScript passes; 75 focused chat, project-memory, graph-context, provider and subscription tests pass. These tests use isolated data and simulated providers; no personal project or source data was changed by this work. Real-model retesting and installation are tracked separately by the main acceptance run.

### Phase 3 — account integration, live quality checks and installation

- Added an official Codex app-server bridge for ChatGPT subscription authentication and inference. Codex manages sign-in and token refresh; Myynd stores only the user's provider selection and the consenting account identity. It does not copy OAuth tokens into its configuration or silently fall back to API billing. Chat, Brief, feed generation, news, memory and automation reasoning share the same selected engine.
- The native bridge uses ephemeral restricted sessions, disabled native tools/apps/MCP servers, no project instructions or inherited API keys, bounded cancellation and cleanup. MCP configuration is refreshed before each session. The app-server JSON override API requires nested objects to preserve literal MCP names; a real regression trial caught and corrected the incompatible TOML-quoted dotted-key form before installation. A changed native ChatGPT identity requires reselecting the current account in Sources.
- Sources now shows the existing ChatGPT account, explicit selection, browser sign-in when needed, and bounded status polling. Preferences identifies the active provider and its off state. Unknown internal plan codes are not shown as customer-facing plan names. Switching or disabling providers retains stored API credentials; disabling ChatGPT does not log out the native Codex account or select a paid API provider.
- Anthropic's current official Claude Code compliance documentation does not support third-party products offering Claude.ai subscription login without approval. Removed the misleading new-connection promotion for that legacy path, retained API access and existing selected legacy status, and linked the provider's explanation. No new Claude.ai OAuth flow is claimed or implemented.
- The current profile and the named pre-0.2.12 backup contained no saved Claude API key. No prior key was recoverable from those two locations. Persistence fixes cover observed code-level loss paths, but this does not identify which historical event removed the user's key. No secret was displayed or inserted into the live profile during testing.
- A real managed ChatGPT account was detected without a model call. Real model trials then verified a plain reply and a complete Myynd function-call/result/reply round trip. The account-selected default model was `gpt-5.6-sol`.
- A real current-project question against a private snapshot of the user's actual data initially misrepresented an old school announcement as fresh evidence. Fixed both the missed “what should I work on next” intent and later chat searches. The repeat completed in 10 seconds, used zero historical citations, proposed a bounded H-Farm workflow/pilot exercise grounded in the saved goal, and explicitly stated that current commitments were unverified. This remains a proposal, not evidence of an assigned deliverable or an action performed.
- In the private snapshot, an explicit H-Farm goal correction was acknowledged only after saving. A separate fresh process read the exact corrected goal and the same four canonical projects. The live personal goal was not changed by this fixture.
- Moved chat-title generation after the main reply so it cannot occupy the local model's queue first. Failed chat requests now retain the user's text instead of deleting it, preserving the request through a restart. The earlier failed request had already been deleted by the old code and was not recovered.
- Full combined suite: **1,195 tests, 1,177 passed, 18 skipped, zero failures**. Production build and all three TypeScript configurations passed. After the final native configuration correction, all eight ChatGPT bridge tests and server typecheck passed again. Cancellation coverage includes waiting beyond the former 15-second cutoff without aborting or duplicating a local-model request, plus explicit cancellation, setup cancellation and late-thread cleanup.
- Backed up the original account and desktop profile privately under `~/.myynd-backups/20260914-before-0.2.13`, retained the prior application bundle there, and installed **Myynd 0.2.13** in `/Applications/Myynd.app`. The installed bundle passes strict deep signature verification. All **93 runtime/web files** match the workspace/build outputs exactly.
- Native UI acceptance: selected the existing ChatGPT account in Sources; verified “In use”; turned it off and confirmed the saved provider remains ChatGPT with `attivo:false`, while the prior compatible-provider settings remain intact; then re-enabled it. No API key was selected as a fallback.
- Native chat acceptance: “Can you fix my projects and goals?” promptly requested the missing exact values without timing out or falsely claiming a save. A follow-up H-Farm next-step question returned a short proposal and explicitly reported no current assigned request in linked sources. No task or outgoing message was created by that check.
- Native map acceptance: `Beer.xlsx` shows its original Downloads location, its September 2025 date, readable source contents, neighboring documents and the explicit “Reference material” classification. The map explains that shared-word links are associations and that relevance separately determines tasks.
- Quit and reopened the installed app to check persistence. The native bridge exited with Myynd rather than leaving an orphaned inference process. The final post-restart observations are recorded below.

Official integration references: https://learn.chatgpt.com/docs/app-server and https://code.claude.com/docs/en/legal-and-compliance (reviewed 14 September 2026).

Final post-restart result: the original Myynd account remains signed in, Sources again shows the same ChatGPT account **In use**, and on-disk selection, consent identity and previous compatible-provider settings remain present. The displayed indexed mail count changed from 36 to 31 during this interval; the earlier attention audit counts are explicitly time-stamped and are not presented as a permanent mailbox total. The attention feed remains empty. Left Sources open with the active ChatGPT connection visible.

### Phase 4 — customer-owned sign-in and chat latency (15 September 2026)

- Installed **0.2.14**. This supersedes phase 3's reliance on a pre-existing computer-wide Codex login: the desktop bundle now includes the official pinned 0.145.0 runtime and uses a private, persistent account directory for each Myynd profile. Customers sign in from Sources in their browser; no separate CLI installation or API key is required. Existing global OAuth credentials are not copied into Myynd. Export does not include the runtime account directory.
- Login completion and cancellation track the exact login ID for the current profile. An older account cannot falsely complete a new sign-in. Cancellation handles completion races, and child processes stop when Myynd exits. The selected provider does not silently fall back to a paid API.
- Interactive chat chooses the faster model only when the account's actual model catalog includes it. Background and delegated work retain the account default. Real answer text streams from the guarded response envelope, without exposing JSON or tool arguments. Initial retrieval is smaller; later searches can still expand relevant sources. Greetings and named saved-goal reads answer directly from local state.
- Measured private-snapshot workflow trials: generated first text improved from **8,784 ms** to **3,994 ms** for project planning (5,291 ms total), and **4,300 ms** for a reference-material explanation (5,267 ms total). A named saved-goal read took **5 ms**. These are observed samples, not latency guarantees. Generated trials preceded account isolation and used the then-authorized account; fresh customer OAuth completion was not performed.
- Anthropic's current documentation permits end users to authenticate to unmodified Claude Code for coding, but does not support routing Claude subscription credentials through a general third-party intelligence application. Help now explains that distinction. General Myynd reasoning still uses Claude API credentials when Claude is selected; no fake Claude subscription sign-in is offered. References: https://learn.chatgpt.com/docs/app-server and https://code.claude.com/docs/en/legal-and-compliance.
- Verification: **1,216 tests, 1,198 passed, 18 skipped, zero failures**; all TypeScript checks and production build passed. Packaged macOS arm64 runtime reports 0.145.0. Installed application passes strict deep signature verification; all **95 runtime/web files** match the workspace/build. Release is locally ad-hoc signed, not notarized.
- Backup of the previous app, account data and desktop profile is private under `~/.myynd-backups/20260915-before-0.2.14`. Installation used a clean bundle replacement.
- Native acceptance on the installed version: Sources shows the fresh self-service sign-in control; it opens **auth.openai.com**, displays a pending state, and cancels correctly. No credentials were entered. The user must complete their own new Myynd sign-in before generated chat, briefing and other model work resume; full OAuth completion and real-token persistence are therefore not claimed as verified. Isolated tests cover persistent account paths and separation between profiles.
- Native chat promptly answered “Hello” and returned the actual saved H-Farm goal: “I want to help to solidify AI Systems to Streamline company efforts.” This verification did not modify the goal. After quitting and reopening, the original account and conversation remained, as did the selected provider, ChatGPT selection and compatible-provider settings. Left Sources open for customer sign-in. The feed still shows no actionable items; the existing expired briefing reports a provider error while the new account is not connected. This is not a claim that the user's entire real workload is empty.

### Phase 5 — real native deliverables, working Aurora, connection investigation

- Added a bounded native document action for delegated writing: Myynd opens Pages or TextEdit, creates the complete document, saves a unique native file, compares the app-read body with the requested body, and verifies the saved artifact. Native commands are fixed implementation code; model output is JSON document data, never executable script. Unsupported environments/apps produce explicit errors. This is not general control of arbitrary applications.
- Manual delegations may use this action; unattended automation recipes do not acquire native write permission. Indexed documents remain evidence, not authorization. Prompt-only mode still produces a prompt. A verified native delivery bypasses speculative model classification and email preparation. A prose-only success claim cannot complete an app-writing task. Essential questions remain questions. Page-count requests constrain generated word count; exact layout still depends on the native document.
- Added abort propagation through the delegated model request and native action, so recall/reassignment prevents later actions and late success. A document already saved at cancellation is not silently deleted. Active worker steps are replayed only to the owning account on stream reconnection.
- Added animated multicolor Aurora to both expanded feed task cards and collapsed rows. It follows real worker events, distinguishes queued work, stops on terminal states, and honors reduced motion. Added Collapse even for a lone task. Native visual acceptance confirmed both shapes during the actual essay run.
- Verified deliverables now have stored app/title/path metadata and an Open in Pages/TextEdit button. Reopening resolves the stored path inside the current account's deliverables folder; the renderer does not submit arbitrary paths. Added migration, persistence/isolation tests, and metadata clearing on a new run.
- Anthropic investigation initially reproduced an uncertain startup-auth result incorrectly shown as disconnected; unknown status now remains checking, with bounded retries, shared concurrent checks, and a manual retry. The actual before/after configuration reset during verification was then traced to an existing unisolated disconnect test writing the real profile. Fixed the affected test and added a per-worker temporary-profile bootstrap for the entire suite. Updated legacy HOME-based fixtures to explicitly select their test data. Restored only the known prior Claude connection preferences; selected ChatGPT and authentication credentials were unchanged. The isolated suite leaves the live profile unchanged. Native restart verification shows Anthropic Connected, with the existing account connection, without reconnecting.
- Notes had multiple conflicting hardcoded colors rather than a discovered random generator. Sources icons (including disconnected state), knowledge-map source metadata, and automation badges now share the fixed canonical #5B9BC9 value. Existing Notes disk-access status is separate and was not changed.
- Final production build/typechecks passed. Full isolated suite: 1,248 tests, 1,230 passed, 18 skipped, zero failures; an additional test-bootstrap isolation regression passed separately. Installed Myynd 0.2.15 passes strict deep signature verification and all 98 runtime/web files match the final workspace/build. Private backups retain the prior bundles, data and desktop profile; latest pre-final backup: ~/.myynd-backups/20260915-155917-before-0.2.15-final.
- Native action acceptance: isolated Pages and TextEdit documents were created and verified. More importantly, the installed Myynd executed the user's existing AI/quantum-computing essay task through the selected real ChatGPT provider and produced an actual saved Pages document. The first run produced three pages for the requested two; this exposed the need for a tighter writing-length budget. The second run and direct artifact opening are recorded below after completion. No outgoing message was sent and no existing customer document was overwritten.

Final native acceptance: the second real delegated run produced a **636-word, two-page Pages document**, visibly verified in Pages at `~/.myynd/deliverables/document-jEHPdX/The Future of AI in Relation to Quantum Computing.pages`. Myynd showed Ready and **Open in Pages** only after native verification. Clicking that button opened the exact saved file. The Aurora was visible during execution in the expanded card and, after Collapse, in the row beneath the briefing; it stopped when the task became ready. Sources and the Anthropic detail panel showed the retained account connection after restart without manual reconnection. Left the finished document open in Pages for review.

### Phase 6 — styled deliverables and evidence-based visual review (15 September 2026)

- Added a shared production brief for delegated work. It separates explicit style preferences from observations, ranks references by purpose/topic and recipient, avoids treating incoming emails as the user's voice, and excludes receipts, promotions, CVs, agent instructions and generated deliverables from style learning. Font evidence is read from real DOCX/RTF metadata in connected folders; missing evidence uses an explicitly labeled editorial default. This is bounded reference selection, not a claim of perfect personal-style inference.
- Pages documents now import editable, structured OOXML with a real title, section hierarchy, readable serif type, paragraph spacing, margins, widow protection and page numbers. Myynd saves the native Pages file, reads its text back, exports that actual document as PDF, and renders every page. The layout is not a separate approximation of the delivered file.
- A separate model review receives the actual page images and checks readability, hierarchy, clipping, overlap, page breaks, continuity and consistency. Every page needs a concrete observation. Review reports retain image hashes and dimensions. Malformed, incomplete, unsupported or failed reviews cannot pass. Actual requested page counts are checked. A failed review can request one complete revision; unresolved issues remain marked for review.
- The selected ChatGPT transport now passes image inputs through the official runtime. No fallback to a paid API was introduced. Claude API and ChatGPT image review are supported; other providers and plain TextEdit deliveries remain explicitly unverified.
- Task cards show a compact document summary, page count, style origin, review result/issues, Open in Pages and Preview PDF. Both expanded and collapsed views use the stored delivery metadata. Previously generated documents without review evidence are labeled unverified. Active tasks periodically reconcile with persisted state so a missed stream event cannot sustain a false working indicator.
- The first installed-app acceptance run exposed `DOMMatrix is not defined` in Electron's utility process, despite successful standalone rendering. Moved PDF rendering to a bounded, cancellable worker that explicitly selects the Node canvas backend inside Electron. Verified the fix both in an Electron utility process and against the packaged ASAR. A saved native file now remains available when rendering fails; missing page images cannot become a passed review.
- Full isolated suite after the renderer correction: 1,270 tests, 1,252 passed, 18 skipped, zero failures. Production build and all TypeScript configurations passed. Native graphical acceptance and final installation are recorded below.
- Scope: the shared production brief includes criteria for email, presentations, video and agent-training tasks. This release's automatic render–inspect–revise execution is implemented for Pages documents. It does not claim working video, Keynote or Hermes-training executors.

Final installed acceptance: **Myynd 0.2.16**, strict deep signature verified, all **102 runtime/web files** match the final build. Private backup: `~/.myynd-backups/20260915-162403-before-0.2.16-final`. Retried the user's existing essay using Myynd's own task button and selected ChatGPT subscription. It created a **679-word, two-page Pages document**, exported the real PDF, rendered both pages, and received a complete passing visual review from `gpt-5.6-sol`. Independently inspected both rendered pages: clear black title and section hierarchy, Georgia body text, readable leading, consistent margins, page numbers, no clipping or overlap. No source document with reliable personal typography was selected, so the UI honestly says Editorial. The stored review contains observations, hashes and dimensions for both pages.

The final document is `~/.myynd/deliverables/document-Bb9Llg/The Future of AI and Quantum Computing.pages`, with `Preview.pdf` beside it. Native UI showed Pages · 2 pages · Editorial · Review passed. Verified Open in Pages opens that exact saved native path and Preview PDF opens the exact two-page export. The collapsed working card showed Aurora, the expanded completed card stopped animating, and Sources retained Anthropic Connected and OpenAI Connected after the update. No outgoing communication was sent.

Final restart check: the same essay remained Ready with Pages · 2 pages · Editorial · Review passed and both opening controls. The review status and delivery links persisted. Focused final UI/review-label checks passed after the active-state reconciliation change. Left Myynd on the verified delivery card; the native Pages document and PDF are also open.

### Phase 7 — calm completion, contextual revisions and bounded initiative

- Compact completed delivery cards now say “It's done” with short, truthful save-location text, Open and Discuss actions; review/page metadata is available under Details. Only passed native deliveries use this completed treatment. Active expanded/collapsed tasks use a substantially lighter mint/lilac/peach Aurora, dark readable text, broad slow motion, and reduced-motion support.
- Passed native documents are copied without overwrite into one Desktop/Myynd folder. Provisional and failed-review drafts stay private. Stored desktop metadata controls opening and wording; failure to copy never becomes a false desktop claim. Earlier native versions remain intact.
- Discuss opens an unsent chat tied to the exact task, preserving that association across relaunch. Explicit current-message feedback can immediately start a real revision, including subscription/local-provider paths. Praise/questions, invented feedback and mismatched targets cannot start revisions. Each revision is a child task with the prior generated source as reference and the previous artifact preserved. Revised page counts override the original requirement, including number words. External edits in Pages are not automatically synchronized into that generated-source snapshot.
- Added opt-in Prepare ahead in Automations. Uses current selected provider and memory to prepare unsent replies for fresh, direct email requests. Two attempts per rolling day, at least three hours apart, at most two outstanding proactive drafts; explicit work gets capacity first. No native apps, desktop files, or sending permissions. It honors Ask first, dismissals, done/deleted sources, answered threads, and source changes during generation. Obsolete work is quietly withdrawn without fabricating user feedback. Duplicate source feed cards hide while a prepared task is present.
- Preparation runs after startup, every fifteen minutes, and on wake while Myynd is running. It does not wake a sleeping Mac or execute while the app is closed. The setting is off by default for other customers; this user explicitly requested activation.
- Full isolated test run: 1,286 total, 1,268 passed, 18 skipped, zero failures; final additional focused initiative checks also passed. All three TypeScript configurations and production build pass. Myynd 0.2.17 installed with strict deep signature verification; all 104 runtime/web files match the workspace build. Private backup: ~/.myynd-backups/20260915-191911-before-0.2.17. Native acceptance follows below.

Live acceptance caught and fixed additional integration issues: the preparation toggle duplicated Content-Type casing and Express skipped its JSON body; a real Request transport test now covers both toggle directions. General chat revision acknowledgements previously concatenated two model rounds; successful single revisions now return one truthful acknowledgement. Revision cards now hide internal task/reference instructions. Intent detection was narrowed so praise about how a document “uses” headings, explanatory questions and requests for suggestions cannot trigger edits.

The graphical reviewer correctly withheld completion for duplicate old/revised titles, split paragraphs, unbalanced pages and a third-page spill. Added revised-title de-duplication, paragraph keep-lines, tighter short-document word budgets, and estimated balancing at paragraph boundaries for an explicit two-page brief. Actual PDF page count remains authoritative. A native Pages export using the corrected layout produced two balanced pages; both were independently inspected, then the real selected ChatGPT image reviewer passed both. Review feedback now follows the app language. App-produced Desktop/Myynd outputs are excluded from fresh attention and style-reference inference to prevent self-generated work loops.

Latest full isolated run before the final pagination addition: 1,292 tests, 1,274 passed, 18 skipped, zero failures. After pagination, 52 relevant layout/revision/attention/Claude tests passed, along with production build/typechecks. Final release bundle again matches all 104 runtime/web files and passes strict deep signature verification. Latest backup: ~/.myynd-backups/20260915-193456-before-0.2.17-release.

Prepare ahead was enabled through the native UI. Prepare now found no qualifying new request in the real sources and created nothing. Existing unrelated automation stayed paused. The light Aurora was visually verified in both expanded and collapsed task cards. The original completed essay was preserved; chat created a linked revision task rather than reopening or replacing the original.

Final native acceptance uncovered an actual startup stall: worker timing showed immediate queue dispatch, ~1.1 seconds for retrieval, then an indefinitely pending optional typography reference lookup. The production brief now has a shared 1.5-second reference-reading budget, cancellable reads/parsing and safe late-promise handling. Saved explicit preferences still win; an unavailable reference falls back to editorial defaults. The worker immediately reports Preparing your task and logs only stage timings/IDs, not private document contents. In the installed retry, generation began after **2.68 seconds**, resolving the observed multi-minute stall.

After these corrections the full isolated suite reports **1,297 tests: 1,279 passed, 18 skipped, zero failures**. Production build/typechecks pass. Installed Myynd 0.2.17 again passes strict deep signature verification and all **104 runtime/web files** match the workspace build; packaged package metadata differs only in builder-removed scripts/dev dependencies.

The final live revision completed in **32.4 seconds**, including native Pages creation, PDF rendering and model visual review; model generation began after 2.68 seconds. It produced a **600-word, two-page** document with shorter paragraphs and concise section headings. Independently inspected both actual page PNGs: readable hierarchy, consistent margins, no duplicate title, no clipping or overlap. Fixed comma-separated revision-title deduplication after a real failed review; calibrated review instructions to distinguish actual defects from optional page-filling/continuation-heading preferences. All ten focused layout/review tests and final build passed after this last adjustment.

Passed delivery: `~/Desktop/Myynd/The Future of AI and Quantum Computing - Revised-C7DDSq.pages`. The original completed essay remains intact. The native completion card is approximately 105px tall and says “It's done. Saved to your desktop. Take a look and tell me what you think in chat.” Verified Open in Pages opens that exact Desktop file, not the private working copy. Discuss opens an unsent composer prefixed with the exact document title and associated task. No extra message was sent. Prepare ahead remains enabled across relaunch; no eligible fresh email existed, so no real proactive draft is claimed.

Final restart acceptance passed: the compact completed card, exact Desktop delivery link, and passed review persisted. Left Myynd displaying the completed revision. No additional source credentials were requested or altered during this phase.

### Phase 8 — one visible delivery and drafts in Apple Mail

User reported many open Pages iterations and clarified that email drafts belong in Apple Mail. Closed six unchanged private/test essay windows, keeping the one passed Desktop file open; no user-edited documents or saved versions were deleted. Native production now avoids activating each working copy, closes the exact newly saved document after export (including saved export failures), and opens only the final passed delivery. A real Pages save/render check confirmed the working document closes and the existing final document remains the sole open Pages document.

Mailbox draft publication writes unsent IMAP Drafts (and supports connected Gmail drafts), with a durable task reservation preventing duplicates after retries/uncertain writes. It verifies original source message identity and reply recipient before writing. Outlook's current read-only connection cannot publish drafts and reports that limitation. The app shows actual save status and opens the mailbox message; it never sends these drafts. Desktop link handling now permits only narrowly validated message-ID links to Mail, alongside the existing web links.

Live mailbox acceptance created one clearly labelled self-addressed test draft and reused its identifier on repeated save. Apple Mail showed the exact message in Bozze – Donadon with correct body, sender and recipient. No email was sent. Cleanup targets only this verification draft. Full isolated suite at this point: 1,303 tests, 1,285 passed, 18 skipped, zero failures.

Mailbox publication also covers explicitly delegated reply drafts with a verified original source. The live verification draft was removed by its exact unique Message-ID; Drafts returned to the two pre-existing messages. Final full isolated suite: **1,305 tests, 1,287 passed, 18 skipped, zero failures**, with production typechecks/build passing. Pre-update private profile backup: `~/.myynd-backups/20260915-203045-before-0.2.18`.

Remaining capability gaps identified from implementation: (1) a managed isolated checkout/copy executor for GitHub and Hermes, with tests, readable changes and rollback; the shared GitHub tool is read-only and the shared Claude project tool only returns a plan, although separate user-triggered project execution exists; (2) reconciliation with manual edits in Pages/Mail before revision; (3) one stable visible artifact identity across successive user-requested versions, retaining private history; (4) explicit project state/dependencies plus evidence freshness, rather than general keyword retrieval; (5) background execution after the app is quit/Mac sleeps (current schedules require a running app and awake Mac); (6) domain-specific completion checks for slide/video/agent execution, not just content/Pages visual review. These are recommendations, not claims of implemented capability.

Installed **Myynd 0.2.18** and relaunched successfully. Strict deep signature verification passed; all **106 packaged runtime/web files** match the workspace build. Native Automations shows Prepare ahead Active with the updated truthful mailbox-Drafts wording. The test draft is gone and the single final essay remains available. No outgoing email was sent.

## Phase 9 — verified execution, current edits, project evidence and recovery (2026-09-15)

Implemented connected-local-project execution in a managed per-account copy, preserving the original working tree. Claude Code edits the copy with shell/network tools disabled; independent package test/typecheck verification records changed files, hashes, exit status and report location. Unknown verification stays unverified. Task route now marks work delegated before running (fixing discarded results), rejects concurrent dispatch, and does not mark failed/unverified execution ready. The panel exposes verification and opens only a copy backed by that account's action record. No Hermes connector or arbitrary Supabase/database executor is claimed.

Revisions read the current native document (including available unsaved text and paragraph styles) or exact provider draft, capture a baseline, and recheck before publication. Saved document/package hashes detect persisted formatting edits. Gmail revisions update the draft; IMAP replacements require UIDPLUS and UIDVALIDITY, check the exact old draft again and delete only that UID. Ambiguous outcomes do not retry automatically. A worker conflict becomes a visible needs-attention event. Normal child tasks are not revisions.

Project goals, notes, explicit chat decisions and real work outcomes have persisted provenance and superseded history. Source observations cannot become user goals; outdated source evidence is withheld. Project UI exposes current and previous evidence. Literal quotation plus explicit decision intent is required for chat decision writes.

Opt-in quiet preparation now covers direct replies, current unpaid invoices with evidence, source-visible form fields, and explicit requests connected to active projects. It remains capped at two preparations daily and does not claim actual website form filling, payments, submission or arbitrary tool execution. An explicit reversible sender-rule control archives future mail from exact named addresses while Myynd runs; it is not a provider-level sender block and never deletes mail. No actual sender rule was configured during development.

Background scans have durable interval claims and coalesced catch-up after wake/restart; active work holds a scoped app-suspension lease. At most two interrupted proactive draft jobs recover, once each, after consent/source/provider revalidation. Native/manual jobs are not blindly replayed. Physically sleeping or powered-off Macs cannot execute local work.

Calendar creates use stable per-task markers and actual event-property/UID readback. Partial batches can retry without deliberately duplicating prior events; creation is not treated as a transaction. Date validation respects explicit offsets and rejects invalid dates.

Verification: real installed Claude Code edited a disposable project copy, independent npm test passed, and original remained unchanged. Isolated HTTP tests covered saved results, duplicate dispatch and cross-account copy access. Full suite before final native/style refinements: 1,328 passed, 18 skipped, 0 failed. Final suite/package/native acceptance recorded below when complete.

### Final release verification — 0.2.19

Installed and restarted `/Applications/Myynd.app`. Final suite: **1,370 tests, 1,350 passed, 20 skipped, zero failures**. Production build and strict recursive code-signature validation passed; all **121 packaged runtime/web files** match the workspace. Backup: `/Users/tobiadonadon/.myynd-backups/20260915-211514-before-0.2.19`.

Real native checks passed: installed Claude Code edited/tested a disposable copy without changing its original; Pages read back an edited open fixture and rejected a stale baseline; IMAP replaced one disposable draft, verified its body and removal of the old UID, and confirmed no tagged test drafts remained. No mail was sent, and no fixture Pages windows remain. Calendar AppleScript was compiled successfully without creating a live event; calendar mutation semantics were tested through the injected executor.

Restarted UI was visually checked for sender controls, project provenance, and connected Anthropic/OpenAI status. Sources copy was corrected to describe enabled actions honestly. Notes still requires macOS disk access; this permission was not silently changed.

A live read-only chat check exposed an existing deterministic parser bug: a negated request containing “change … projects” plus a colon created a false project. Added exact English and Italian regressions, anchored write intent, question exclusion, and explicit read-only/negation veto. Quarantined and removed only the erroneous test project and its two-message test chat. Repackaged, restarted, and repeated the same question: Myynd correctly recalled the existing H-Farm goal, and a digest of all five project records proved the registry unchanged. The successful read-only check remains visible in Chat.

Limits remain explicit: a connected local Hermes/repository folder can use isolated execution; there is no dedicated Hermes connector, general Supabase database executor, or arbitrary live website form filler. Sender rules archive future incoming mail while Myynd runs; they are not provider-level blocks. Local overnight execution requires the computer awake; missed scans catch up after wake.

## Phase 10 — current scans, project questions, stable identity and scoped agents (2026-09-16)

Notes used a hard-coded blue override, including when its icon was meant to inherit a disabled color. Its fixed source color is now the same dark neutral family as the source UI. The current Notes diagnostic opens the exact database and any present WAL/SHM sidecars without reading their content; absence and I/O errors are separate from permission denial. Returning from Settings refreshes that diagnostic. The connector no longer silently omits a denied sidecar and reads a stale database copy.

The previous installed main app and utility helper had ad-hoc designated requirements tied to changing code hashes. Local builds now pin the existing Tobia Apple Development identity through an ignored machine-specific configuration; distribution signing remains a separate CSC_NAME/CSC_LINK path. The first certificate-backed package and helper have stable certificate-based requirements and strict verification passed. This is a local development signature, not a notarized distribution release. A previous ad-hoc Full Disk Access grant may need one macOS reauthorization when changing identities; the app cannot grant itself this permission.

Manual Read now now refreshes configured sources under the shared synchronization lock before feed generation. A failed source read cannot be reported as an all-clear. Project progress has a separate nonurgent feed lane: actual open project tasks, or a focused question about the next result. Explicit active goals with completed steps ask whether the goal is complete rather than recreating the steps. Inferred projects, dismissed-only work and closed goals are excluded. Per-goal dismissal persists; opening a conversation only prefills an editable prompt. A read-only copied-profile audit produced one grounded question from the real project state.

Local execution now detects Claude Code and Hermes explicitly. Hermes is a bounded selected-file patch adapter in the same managed copy, with a temporary isolated profile and no shell/file/network tools; it does not drive the Hermes app, modify live agent memory, or train model weights. It does not clone rotating OAuth refresh state. The installed Hermes CLI was detected, but a real tiny smoke run failed authentication and changed neither original nor copy. Do not represent it as authenticated or successfully executed on this machine.

Final team execution, suite results and installed acceptance are recorded below after completion.

Team execution is an optional worker + read-only Claude reviewer, sequential and sharing exactly one managed copy. It requires user-written acceptance criteria; independent tests precede review. The reviewer receives bounded changed-file evidence, runs without tools/hooks/MCP/session persistence, and must return a strict verdict with concrete findings. Missing, rejected, failed or incomplete review prevents Ready. Recall/delete/close/manual edits abort active project jobs, and version guards reject late results.

The real Claude reviewer probe failed with an expired OAuth session that could not be refreshed. Independent `claude auth status` confirmed loggedIn=false/authMethod=none. No source/copy file changed in the failed probe. Installed/compatible runtime and authenticated account are now separate prerequisites; a connected API source is not a Claude Code subscription login. Hermes explicitly reports that its isolated adapter needs inference credentials in its existing .env and cannot reuse an OAuth-only login.

Full protected suite at this point: **1,405 tests, 1,385 passed, 20 skipped, 0 failed**. Production build passed. Manual scan errors now remain visible on the feed until a successful scan instead of disappearing with a toast.

First installed acceptance: Notes now renders in fixed neutral dark ink. macOS Settings shows the existing Myynd Full Disk Access toggle enabled, but the new signed app's exact Notes probe remains denied. Requested the user replace the old Myynd permission entry with `/Applications/Myynd.app`; this step is still pending, so Notes access persistence is not claimed verified. The first signed installed package matched all 125 runtime/web files and its designated requirement matched the earlier independently built signed package.

The actual chat saved the user's explicitly requested Myynd goal immediately, and it survived reload as “Finish Myynd.” The live feed now surfaces that goal and a separate H-Farm completion question without recreating completed tasks. Discuss in chat was checked: it prefills an editable prompt and sends nothing. Native acceptance caught two UI gaps, now corrected: keep Read now available when project questions exist, and refresh feed state after chat commits a goal. Sidebar counts include the new lane.

Post-review protected checks: 36 focused tests passed, including runtime/reviewer/route cancellation, project initiative feedback, fresh scans, signing and localization. Production typechecks/build passed after the UI fixes. Final installation and manual scan verification follow.

### Final installed acceptance — 0.2.20

Final package installed and restarted. Strict recursive signature verification passed and all **125 runtime/web files** match the final workspace/build. Backup: `/Users/tobiadonadon/.myynd-backups/20260916-144331-before-0.2.20`. Certificate-based designated requirements remained stable across independently built packages.

Verified in the native UI: sidebar and headline both count two project questions; Read now remains available; the Myynd goal survives restart; a goal refinement sent through chat appears immediately in the feed without reload; Discuss in chat only prefills a draft. The final saved goal is “Finish Myynd with useful intelligence and reliable delegated execution,” expressing the user's stated objective. No external message was sent and no project execution was falsely marked complete.

Actual Read now refreshed the configured sources and returned explicit failures: My Mac's read was incomplete and Notes was unavailable. The persistent error remains visible alongside the useful project questions. This is not a verified all-clear or a claim that these source-access issues are resolved. Notes still needs the user to replace the prior Full Disk Access entry for the signed `/Applications/Myynd.app`; Claude Code needs its expired session renewed; this Hermes adapter needs supported inference credentials. Those external setup steps remain pending. General Hermes GUI training, model-weight fine-tuning and an arbitrary parallel agent flock are not implemented; team mode is a bounded sequential worker + independent reviewer for local copied projects.
