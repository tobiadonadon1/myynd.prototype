[
  {"js": "(async () => { const h = { authorization: 'Bearer ' + localStorage.getItem('myynd.token'), 'content-type': 'application/json' }; for (const [id, testo] of [['p7-pilota', 'When does the Harbor pilot start?'], ['p7-lisbona', 'What is the rent for the Lisbon office?'], ['p7-citata', 'What did Nora write about the fee for the first phase?'], ['p7-due', 'What was confirmed about the Harbor pilot kickoff?'], ['p7-clausola', 'What are the date and the fee of the Harbor pilot?']]) { const c = await (await fetch('/api/chat/' + id, { headers: h })).json(); if (Array.isArray(c) && c.length) continue; const r = await fetch('/api/chat/' + id, { method: 'POST', headers: h, body: JSON.stringify({ testo }) }); await r.text() } const t0 = Date.now(); while (Date.now() - t0 < 20000) { const l = await (await fetch('/api/chat', { headers: h })).json(); const titoli = (Array.isArray(l) ? l : []).map(c => c.titolo || ''); if (['Harbor pilot start', 'Lisbon office rent', 'Nora on the fee', 'Kickoff confirmed', 'Date and fee'].every(t => titoli.includes(t))) return 'chat pronte, con i titoli'; await new Promise(r => setTimeout(r, 500)) } return 'chat pronte, ma senza titoli dopo 20 s' })()"},
  {"aspetta": 3000},
  {"js": "(() => { location.reload(); return 'ricarico: le chat nate dopo l’apertura entrano nell’elenco' })()"},
  {"aspetta": 6000},
  {"vai": "Chat"},
  {"clicca": "Harbor pilot start"},
  {"aspetta": 1500},
  {"scatta": "risposta"},
  {"testo": "risposta"},
  {"js": "(() => { const e = document.querySelector('[aria-label^=\"Source:\"]'); if (!e) return 'nessun segno'; e.focus(); e.dispatchEvent(new FocusEvent('focusin', { bubbles: true })); return e.getAttribute('aria-label') })()"},
  {"aspetta": 500},
  {"scatta": "sopra-fonte"},
  {"js": "(() => { const e = document.activeElement; if (!e || !/^Source:/.test(e.getAttribute('aria-label') || '')) return 'il fuoco non è sul segno'; let a = e.parentElement; while (a && !/auto|scroll/.test(getComputedStyle(a).overflowY)) a = a.parentElement; if (!a) return 'nessuna colonna che scorre'; a.dispatchEvent(new Event('scroll')); return new Promise(r => setTimeout(() => r('dopo lo scorrimento: ' + JSON.stringify({ fuoco: document.activeElement === e, nuvola: !!document.querySelector('[role=tooltip]'), anello: getComputedStyle(e).outlineStyle })), 300)) })()"},
  {"js": "(() => { const e = document.activeElement; if (!e || !/^Source:/.test(e.getAttribute('aria-label') || '')) return 'il fuoco non è sul segno'; e.dispatchEvent(new KeyboardEvent('keydown', { key: 'Escape', bubbles: true })); return new Promise(r => setTimeout(() => r('dopo Esc: ' + JSON.stringify({ fuoco: document.activeElement === e, nuvola: !!document.querySelector('[role=tooltip]'), anello: getComputedStyle(e).outlineStyle })), 300)) })()"},
  {"scatta": "dopo-esc"},
  {"js": "(() => { const prima = document.activeElement; if (prima) prima.dispatchEvent(new FocusEvent('focusout', { bubbles: true })); const e = document.querySelector('[aria-label=\"From your memory\"]'); if (!e) return 'nessun segno'; e.focus(); e.dispatchEvent(new FocusEvent('focusin', { bubbles: true })); return 'memoria' })()"},
  {"aspetta": 500},
  {"scatta": "sopra-memoria"},
  {"js": "(() => { const prima = document.activeElement; if (prima) prima.dispatchEvent(new FocusEvent('focusout', { bubbles: true })); const e = document.querySelector('[aria-label^=\"Source:\"]'); if (!e) return 'nessun segno'; e.focus(); e.dispatchEvent(new FocusEvent('focusin', { bubbles: true })); return 'fonte' })()"},
  {"premi": "Enter"},
  {"aspetta": 1200},
  {"scatta": "documento"},
  {"chiudi": true},
  {"clicca": "Lisbon office rent"},
  {"aspetta": 1200},
  {"scatta": "rifiuto"},
  {"testo": "rifiuto"},
  {"clicca": "Nora on the fee"},
  {"aspetta": 1200},
  {"scatta": "citata"},
  {"testo": "citata"},
  {"js": "(() => { const e = document.querySelector('[aria-label^=\"Source:\"]'); if (!e) return 'nessun segno'; e.focus(); e.dispatchEvent(new FocusEvent('focusin', { bubbles: true })); return new Promise(r => setTimeout(() => { const n = document.querySelector('[role=tooltip]'); r('nuvoletta sotto la frase citata: ' + (n ? n.textContent : 'nessuna')) }, 400)) })()"},
  {"scatta": "sopra-citata"},
  {"js": "(() => { const prima = document.activeElement; if (prima) prima.dispatchEvent(new FocusEvent('focusout', { bubbles: true })); return 'fuoco via' })()"},
  {"clicca": "Kickoff confirmed"},
  {"aspetta": 1200},
  {"scatta": "due-segni"},
  {"testo": "due-segni"},
  {"js": "(() => { const segni = [...document.querySelectorAll('sup[aria-label^=\"Source:\"]')]; if (!segni.length) return 'nessun segno'; segni[0].scrollIntoView({ block: 'center' }); return new Promise(r => setTimeout(() => { const esiti = segni.map(s => { const b = s.getBoundingClientRect(); const cs = getComputedStyle(s); const pl = parseFloat(cs.paddingLeft), pr = parseFloat(cs.paddingRight); const x = b.left + pl + (b.width - pl - pr) / 2; const y = b.top + b.height / 2; const e = document.elementFromPoint(x, y); const colpito = e && e.closest('sup'); return { segno: s.textContent, giusto: colpito === s, colpito: colpito ? colpito.textContent : (e ? e.tagName : null), zona: Math.round(b.width) } }); r('al centro di ogni cifra risponde il suo segno: ' + JSON.stringify(esiti)) }, 300)) })()"},
  {"clicca": "Date and fee"},
  {"aspetta": 1200},
  {"scatta": "clausola"},
  {"testo": "clausola"},
  {"js": "(() => { const segni = [...document.querySelectorAll('sup[aria-label^=\"Source:\"]')]; if (segni.length < 2) return 'segni: ' + segni.length; const e = segni[1]; e.focus(); e.dispatchEvent(new FocusEvent('focusin', { bubbles: true })); return new Promise(r => setTimeout(() => { const n = document.querySelector('[role=tooltip]'); r('nuvoletta del secondo segno, nella stessa frase: ' + (n ? n.textContent : 'nessuna')) }, 400)) })()"},
  {"scatta": "sopra-clausola"},
  {"js": "(() => { const prima = document.activeElement; if (prima) prima.dispatchEvent(new FocusEvent('focusout', { bubbles: true })); return 'fuoco via' })()"},
  {"clicca": "Your account"},
  {"vai": "Preferences"},
  {"clicca": "AI and costs"},
  {"aspetta": 800},
  {"js": "(() => { const lab = [...document.querySelectorAll('.f-nome')].find(x => /Check answers every week/.test(x.textContent || '')); if (!lab) return 'riga non trovata'; lab.scrollIntoView({ block: 'center' }); return 'riga in vista' })()"},
  {"aspetta": 600},
  {"scatta": "preferenze"},
  {"testo": "preferenze"}
]
