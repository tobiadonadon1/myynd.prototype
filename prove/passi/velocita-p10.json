[
  { "aspetta": 2500 },
  { "js": "JSON.stringify(performance.getEntriesByType('mark').map(m => [m.name, Math.round(m.startTime)]))" },
  { "scatta": "casa" },
  { "js": "new Promise(ok => { const b = document.querySelector('[aria-label=\"Read now\"]'); if (!b) return ok('occhio non trovato'); const t0 = performance.now(); let premuto = null, primo = null; const mo = new MutationObserver(() => { const ms = Math.round(performance.now() - t0); if (primo === null) primo = ms; if (premuto === null && b.getAttribute('aria-pressed') === 'true') premuto = ms }); mo.observe(b, { attributes: true }); b.click(); setTimeout(() => { mo.disconnect(); ok(JSON.stringify({ premuto, primo, disabilitato: b.disabled, riga: !!document.querySelector('[data-riga-che-lavora]') })) }, 300) })" },
  { "scatta": "leggo" },
  { "aspetta": 2500 },
  { "scatta": "scelgo" },
  { "testo": "scelgo" },
  { "aspetta": 4500 },
  { "scatta": "finita" },
  { "testo": "finita" },
  { "js": "new Promise(ok => { const titolo = 'Fix the three Northwind account issues'; const it = document.createTreeWalker(document.body, NodeFilter.SHOW_TEXT); let n; while ((n = it.nextNode())) { if ((n.textContent || '').includes(titolo)) break } if (!n) return ok('carta non trovata'); let e = n.parentElement, b = null; for (let i = 0; i < 8 && e && !b; i++) { b = [...e.querySelectorAll('button')].find(x => (x.textContent || '').trim() === 'Delegate to Myynd') || null; e = e.parentElement } if (!b) return ok('bottone non trovato'); const t0 = performance.now(); const guarda = () => { const carta = [...document.querySelectorAll('[role=button],[tabindex]')].some(x => (x.textContent || '').includes(titolo) && !x.querySelector('[data-working]') && !x.hasAttribute('data-working') && !x.closest('[data-working]')); const riga = [...document.querySelectorAll('[data-working]')].some(x => (x.textContent || '').includes('Northwind account issues')); return { carta, riga } }; b.click(); let via = null, nata = null; const giro = () => { const s = guarda(); const ms = Math.round(performance.now() - t0); if (via === null && !s.carta) via = ms; if (nata === null && s.riga) nata = ms; if ((via !== null && nata !== null) || ms > 3000) ok(JSON.stringify({ cartaVia: via, rigaNata: nata })); else requestAnimationFrame(giro) }; requestAnimationFrame(giro) })" },
  { "scatta": "affidata" },
  { "vai": "Chat" },
  { "js": "(() => { const t = document.querySelector('textarea'); if (!t) return 'niente casella'; t.focus(); t.dispatchEvent(new FocusEvent('focusin', { bubbles: true })); return 'fuoco' })()" },
  { "aspetta": 800 },
  { "scrivi": "When is the audit call with Priya?" },
  { "js": "(() => { const b = document.querySelector('[aria-label=\"Send\"]'); if (!b) return 'niente bottone'; b.click(); return 'mandata' })()" },
  { "aspetta": 2500 },
  { "scatta": "chat" },
  { "js": "(location.reload(), 'ricarico')" },
  { "aspetta": 60 },
  { "scatta": "attesa" }
]
